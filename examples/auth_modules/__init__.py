"""Example authentication modules for browser_launcher.

These modules demonstrate how to implement custom authenticators
by extending `AuthenticatorBase`. They can be registered via Python
entry points in pyproject.toml under the ``browser_launcher.authenticators``
group so the framework discovers them automatically.
"""
