# Poetry

Consult the poetry official site for the install.

Run this so that future poetry installs have its own venv:

```
poetry config virtualenvs.in-project true
```

The config of my machine lives in: 

```
ls "$HOME/Library/Application Support/pypoetry/config.toml"
```